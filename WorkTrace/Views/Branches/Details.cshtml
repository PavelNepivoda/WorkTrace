@model WorkTrace.Models.Branch
@{
    ViewData["Title"] = "Detaily pobočky";
}
<h1>Detaily pobočky</h1>
<div>
    <dl class="row">
        <dt class="col-sm-2">Název</dt>
        <dd class="col-sm-10">@Model.Name</dd>
        <dt class="col-sm-2">Adresa</dt>
        <dd class="col-sm-10">@Model.Address</dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Upravit</a>
    <a asp-action="Index" class="btn btn-secondary">Zpět na seznam</a>
</div>